# .streamlit/secrets.toml

# ===================================
# Kinyarwanda Focus Group Transcriber
# Secrets Configuration File
# ===================================

# Instructions:
# 1. Copy this file to a new file named `secrets.toml` in the `.streamlit` directory.
#    - The final path should be: `.streamlit/secrets.toml`
# 2. Fill in the required API keys and settings below.
# 3. DO NOT commit `secrets.toml` to version control.

# --- Google Gemini API Key ---
# Required for AI processing (correction, summarization, translation)
# Get your key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY = "your-gemini-api-key-here"

# --- Admin Users ---
# List of email addresses for users who should have admin access
# These users can manage other users and view system analytics
ADMIN_EMAILS = [
    "admin1@example.com",
    "admin2@example.com"
]

# --- GPU Acceleration ---
# Optional: Set to `true` if your deployment environment has a GPU
# This can significantly speed up transcription
# Default is `false`
USE_GPU = false

# --- Production Authentication (Optional) ---
# For integrating with Google OAuth in a production environment
# [google_oauth]
# client_id = "your-google-client-id.apps.googleusercontent.com"
# client_secret = "your-google-client-secret"
# redirect_uri = "https://your-app-url.streamlit.app" # Or http://localhost:8501 for local dev
# cookie_key = "a-random-string-for-cookie-signing"
